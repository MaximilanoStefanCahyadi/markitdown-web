fastapi[standard]
markitdown[all]
python-multipart

# OCR stack (the Tesseract binary itself is installed by the Dockerfile)
PyMuPDF==1.26.3
pytesseract==0.3.13
Pillow==11.3.0

# Photo scanner (headless build: no GUI system libraries needed)
opencv-python-headless==4.12.0.88
